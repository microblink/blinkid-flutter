name: blinkid_flutter_example
description: Example app demonstrating BlinkID Flutter plugin custom scanner UI.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.12.1
  flutter: '>=3.44.1'

dependencies:
  flutter:
    sdk: flutter
  blinkid_flutter:
    path: ../
  permission_handler: ^11.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
